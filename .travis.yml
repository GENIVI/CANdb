language: cpp
dist: xenial
arch:
  - amd64
  - arm64
os: linux

notifications:
  email: true
  
before_install:
  - sudo apt-get update
  - sudo apt-get -y install libboost-dev

before_script:
  - rm -rf *

script:
  - CMAKE_BUILD_TYPE=Release
  - ./.travis.sh
  - CMAKE_BUILD_TYPE=Debug 
  - ./.travis.sh
    
